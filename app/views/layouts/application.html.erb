<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "GoDateMe App" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "GoDateMe App" %>">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header>
      <%= render 'layouts/navigation' %>
    </header>
    <main role="main">
      <%= render 'layouts/messages' %>
      <h3>GoDate.Me Login Page</h3>
      <div id='portal'>
        <div id='login-section'>
          <h4>Login</h4>
        </div>
        <div id='sign-up-section'>
          <h4>Sign Up</h4>
        </div>
      </div>
      <%= yield %>

      <script type="text/x-handlebars" data-template-name="application">
            <div class='navbar navbar-default'>
      <div class='container'>
        <ul class='nav navbar-nav navbar-right'>
          <button><span>{{#link-to 'index' tagName='li'}}Homepage{{/link-to}}</span></button>
          <button><span>{{#link-to 'users' tagName='li'}}Users{{/link-to}}</span></button>
        </ul> 
        <div class='container'>{{ outlet }}</div>
        <footer class='container'></div>
      </script>

      <script type="text/x-handlebars" data-template-name="users">
        <h2>Users</h2>
        <ul>{{#each users}}
          <p>{{name}}</p>
          {{/each}}
        </ul>

      </script>

    </main>
  </body>
</html>
